// Single source of truth for gameplay/draw constants that must not drift
export const WORLD_GRID_SIZE = 64;
export const DEFAULT_BG_BRIGHTNESS = 0.4;
export const DEV_SUMMARY_FRAMES = 1800; // ~30s @ 60fps
// Combat tuning
export const MISSILE_HIT_DAMAGE = 20;
export const LASER_HIT_DAMAGE = 10; // half the missile hit by design
